export interface SearchContactCardProps {
  id: number;
  firstName: string;
  lastName: string;
  source: string;
}

export interface SearchContentCardProps {
  firstName: string;
  lastName: string;
}
